# Secrets — never commit the Gemini API key
KneeCareApp/agent.env

# Local persistent data (lives on this machine; do not push personal logs)
KneeCareApp/data/*.db
KneeCareApp/data/*.db-journal
KneeCareApp/data/*.db-wal
KneeCareApp/data/*.db-shm
KneeCareApp/data/*.bak-*
KneeCareApp/data/.migrated_from_home

# Python virtualenv for the optional assistant (google-adk)
KneeCareApp/.venv/

# Python caches
__pycache__/
*.pyc

# Stray test database + local dev helper (not part of the app)
KneeCareApp/test.db
seq_editor.py
